spring:
  profiles:
    active: global
  application:
    name: ${spring-eureka.name}
server:
  port: ${spring-eureka.port}
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
